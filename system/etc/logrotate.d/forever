/var/log/forever/*.log {
        daily
        missingok
        rotate 30
        dateext
        dateformat -%Y-%m-%d
        compress
        delaycompress
        notifempty
        #create 0640 www-data adm
        #sharedscripts
        copytruncate
}
